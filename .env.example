# Valores para correr la app desde tu máquina host
# En Docker Compose, el servicio `app` redefine MONGO_URI y REDIS_URL
PORT=4000
MONGO_URI=mongodb://localhost:27017/app
REDIS_URL=redis://localhost:6379
